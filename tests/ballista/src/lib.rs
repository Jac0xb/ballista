// Needed to compile program but tests are in ../tests directory
// This is because the tests use ballista + sdk as dependencies
// If the tests were in the ballista crate then there would be a circular dependency

pub mod ballista_tests {}
